This directory stores release intent files for `invisible-wallet-sdk`.

Create one with:

```bash
npx changeset
```

When merged to `main`, the release workflow will open or update a version PR.
After that PR is merged, the workflow can publish the SDK to npm.
